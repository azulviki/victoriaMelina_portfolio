---
// src/components/Hero.astro
---

<link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght=700&family=Space+Mono&display=swap" rel="stylesheet" />
<link href="https://fonts.googleapis.com/css2?family=Michroma&family=Montserrat:ital,wght@0,100..900;1,100..900&family=Open+Sans:ital,wght@0,300..800;1,300..800&family=Roboto+Condensed:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">

<section id="hero-section" class="hero relative min-h-dvh flex items-center overflow-hidden">

  <!-- El contenedor de texto usa z-20 pero deja pasar los eventos al canvas donde no hay texto -->
  <div id="hero-text-container" class="absolute inset-0 z-20 px-6 md:px-16 lg:px-24 w-full flex items-center justify-start opacity-0 pointer-events-none select-none transition-opacity duration-700 ease-out">

    <!-- En desktop se clava al ancho máximo del h1 (md:w-max) para que todo se alinee perfectamente en caja -->
    <div class="hero-box relative z-30 flex flex-col gap-0 w-full md:w-max pointer-events-none md:pointer-events-auto">

      <div class="hero-top-block w-full flex flex-col gap-0">
        <p
          id="hero-eyebrow"
          class="hero-spread font-mono uppercase text-blue cursor-default text-[0.8rem] md:text-[0.95rem] tracking-[0.5em] md:tracking-[0.4em] mb-4 w-full pointer-events-auto"
          data-es="Realizadora Multimedial"
          data-en="Multimedia Director"
        >Realizadora Multimedial</p>

        <h1
          id="hero-name"
          class="hero-title relative z-40 block mb-2 w-full pointer-events-none"
          style="font-family: 'Montserrat', sans-serif; font-weight: 200; font-size: clamp(2.2rem, 9vw, 6.5rem); line-height: 0.95;"
        >
          <span class="row-name-mobile block md:hidden mb-1">
            <span>V</span><span>I</span><span>C</span><span>T</span><span>O</span><span>R</span><span>I</span><span>A</span>
          </span>
          <span class="row-name-mobile block md:hidden">
            <span>M</span><span>E</span><span>L</span><span>I</span><span>N</span><span>A</span>
          </span>

          <span class="hidden md:flex w-full justify-between gap-2">
            <span>V</span><span>I</span><span>C</span><span>T</span><span>O</span><span>R</span><span>I</span><span>A</span>
          </span>
          <span class="hidden md:flex w-full justify-between gap-2 mt-1">
            <span>M</span><span>E</span><span>L</span><span>I</span><span>N</span><span>A</span>
          </span>
        </h1>

        <p
          id="hero-alias"
          class="hero-spread font-mono tracking-[0.25em] text-violet/60 cursor-default text-[1.2rem] md:text-[1.5rem] mt-2 mb-4 md:mb-6 w-full pointer-events-auto"
        >VJ BLUH</p>
      </div>

      <div class="hero-bottom-block w-full flex flex-col gap-0">
        <p
  id="hero-tagline"
  class="hero-tool-item hero-spread font-mono uppercase cursor-pointer text-[0.95rem] md:text-[1.2rem] mb-6 w-full pointer-events-auto"
  data-es="Creo atmósferas visuales y sonoras"
  data-en="I create visual and auditory atmospheres"
  id="hero-tagline-link">Creo atmósferas visuales y sonoras</p>

        <div
          id="hero-tools"
          class="flex flex-col gap-3 md:flex-row md:justify-between md:gap-4 w-full font-mono uppercase text-white tracking-[0.1em] mb-6 relative z-40 pointer-events-auto">
          <span class="hero-tool-item cursor-pointer block text-left md:text-center" data-link-category="motion">Motion Graphics</span>
          <span class="hero-tool-item cursor-pointer block text-left md:text-center" data-link-category="live">Live Performances</span>
          <span class="hero-tool-item cursor-pointer block text-left md:text-center" data-link-category="sound">Sound Design</span>
        </div>

        <div
          id="hero-tags"
          class="flex flex-row flex-wrap gap-x-3 gap-y-2 md:justify-between w-full font-mono uppercase text-muted/60 tracking-[0.15em] mb-8 relative z-40 pointer-events-auto">
          <span class="hero-tag-item cursor-pointer" data-tag-tool="after-effects">[ After Effects ]</span>
          <span class="hero-tag-item cursor-pointer" data-tag-tool="touchdesigner">[ TouchDesigner ]</span>
          <span class="hero-tag-item cursor-pointer" data-tag-tool="resolume">[ Resolume Arena ]</span>
          <span class="hero-tag-item cursor-pointer" data-tag-tool="ableton">[ Ableton Live ]</span>
        </div>

        <div
          id="hero-nav-links"
          class="hero-title flex flex-row justify-between w-full uppercase tracking-[0.1em] relative z-40 pointer-events-auto"
          style="font-family: 'Montserrat', sans-serif; font-weight: 200;">
          <span class="hero-tool-item hero-nav-link cursor-pointer block text-left" data-scroll-target="sobremi-section">Sobre mí</span>
          <span class="hero-tool-item hero-nav-link cursor-pointer block text-right" data-open-contact-modal>Contacto</span>
        </div>

      </div>
    </div>
  </div>

  <div
  id="hero-tap-hint"
  class="md:hidden absolute inset-0 z-25 flex flex-col items-center justify-center gap-4 pointer-events-none opacity-0">
  <div class="tap-hint-icon relative flex items-center justify-center">
    <span class="tap-hint-ring"></span>
    <svg class="tap-hint-finger" width="40" height="40" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
      <path d="M9 11.5V6.5a1.5 1.5 0 0 1 3 0v5M12 11.5V5a1.5 1.5 0 0 1 3 0v6.5M15 11.5v-2a1.5 1.5 0 0 1 3 0v6c0 3.5-2 6.5-6 6.5s-6-2-7-4l-1.8-3.2a1.4 1.4 0 0 1 2.4-1.4L7 15" stroke="currentColor" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round"/>
    </svg>
  </div>
  <p class="font-mono uppercase text-[0.7rem] tracking-[0.35em] text-white/70">Tocá la pantalla</p>
</div>

 <div 
  id="hero-scroll-zone" 
  class="absolute bottom-0 left-0 w-full h-24 z-50 pointer-events-auto cursor-pointer flex items-center select-none px-6 md:px-16 lg:px-24 text-violet">
  <div id="hero-scroll-visual" class="flex items-end justify-between w-full transition-transform duration-200 active:scale-95">
  <div class="scroll-bar w-px h-12 overflow-hidden">
    <div class="w-full h-full bg-gradient-to-b from-transparent to-violet origin-top"></div>
  </div>
  <div class="scroll-bar w-px h-12 overflow-hidden">
    <div class="w-full h-full bg-gradient-to-b from-transparent to-violet origin-top"></div>
  </div>
  <div class="scroll-bar w-px h-12 overflow-hidden">
    <div class="w-full h-full bg-gradient-to-b from-transparent to-violet origin-top"></div>
  </div>
</div>
</div>

</section>

<style>
  .tap-hint-icon {
    width: 56px;
    height: 56px;
  }

  .tap-hint-ring {
    position: absolute;
    inset: 0;
    border-radius: 9999px;
    border: 1.5px solid rgba(255, 255, 255, 0.5);
    animation: tapHintRing 1.8s ease-out infinite;
  }

  .tap-hint-finger {
    position: relative;
    color: rgba(255, 255, 255, 0.85);
    animation: tapHintTap 1.8s ease-in-out infinite;
  }

  @keyframes tapHintRing {
    0%   { transform: scale(0.6); opacity: 0.9; }
    70%  { transform: scale(1.6); opacity: 0; }
    100% { transform: scale(1.6); opacity: 0; }
  }

  @keyframes tapHintTap {
    0%, 100% { transform: translateY(0) scale(1); }
    15%      { transform: translateY(4px) scale(0.92); }
    30%      { transform: translateY(0) scale(1); }
  }

  .scroll-zone-glow .scroll-bar {
    animation: scrollBarGlow 1.6s ease-in-out infinite;
  }

  @keyframes scrollBarGlow {
  0%, 100% { filter: drop-shadow(0 0 0px currentColor); }
  50%      { filter: drop-shadow(0 0 10px currentColor); }
}

</style>


<script>
  import { gsap } from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';
  gsap.registerPlugin(ScrollTrigger);

   // ─── BANDERA COMPARTIDA DE INTERACTIVIDAD ────────────────────────────
  // En desktop el texto aparece solo, así que es interactivo desde el arranque.
  // En mobile arranca en false: los links (tagline, tools, tags, nav-links)
  // no deben responder a clicks hasta que el usuario reveló el texto con el
  // tap en la scroll-zone Y terminó la animación de entrada.
  let heroMobileInteractive = window.innerWidth >= 768;

  

  // ─── ANIMACIONES DE ENTRADA Y LÓGICA DE REVELADO ─────────────────────────
  const prefersReduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  const isDesktop = window.innerWidth >= 768;
  
  if (prefersReduced) {
    gsap.set(['#hero-text-container', '#hero-eyebrow','#hero-name','#hero-alias','#hero-tagline','#hero-tools'], { opacity: 1 });
  } else if (isDesktop) {
    gsap.set('#hero-text-container', { opacity: 1 });

    const tl = gsap.timeline({ delay: 0.2 });
    tl.fromTo('#hero-eyebrow',  { opacity: 0, x: -16 }, { opacity: 1, x: 0, duration: 0.5, ease: 'power2.out' })
      .fromTo('#hero-name',     { opacity: 0, x: -40 }, { opacity: 1, x: 0, duration: 0.9, ease: 'power3.out' }, '-=0.1')
      .fromTo('#hero-alias',    { opacity: 0, x: -16 }, { opacity: 1, x: 0, duration: 0.5, ease: 'power2.out' }, '-=0.4')
      .fromTo('#hero-tagline',  { opacity: 0, x: -16 }, { opacity: 1, x: 0, duration: 0.5, ease: 'power2.out' }, '-=0.2')
      .fromTo('#hero-tools',    { opacity: 0 },          { opacity: 1, duration: 0.5, ease: 'power1.out' },       '-=0.1')
      .fromTo('#hero-nav-links',{ opacity: 0 },          { opacity: 1, duration: 0.5, ease: 'power1.out' },       '-=0.2');
 
  } else {
    // ─── MOBILE: ESTADO INICIAL ──────────────────────────────────────────
    gsap.set('#hero-text-container', { opacity: 0, pointerEvents: 'none' });
    gsap.set(['#hero-eyebrow', '#hero-name', '#hero-alias', '#hero-tagline', '#hero-tools', '#hero-nav-links'], { opacity: 0 });
    gsap.set('.scroll-bar', { opacity: 0 }); 

    document.documentElement.classList.add('lock-scroll');
    document.body.classList.add('lock-scroll');

    let heroRevealed = false;
    let textAnimationDone = false;
    const scrollZone = document.getElementById('hero-scroll-zone')!;

    if (scrollZone) {
      scrollZone.style.pointerEvents = 'auto';
    }

    // ─── REVELADO DEL TEXTO ───────────────────────────────────────────────
    let glowDelayedCall: gsap.core.Tween | null = null; // ← nueva variable para que desaparezca el glow

    function revealHeroMobile() {
      if (heroRevealed) return;
      heroRevealed = true;

       // Cancelamos el glow: si todavía no había arrancado, que no arranque;
      // si ya estaba corriendo, lo apagamos.
       if (glowDelayedCall) {
       glowDelayedCall.kill();
       glowDelayedCall = null;
      }

  document.getElementById('hero-scroll-zone')?.classList.remove('scroll-zone-glow');
  gsap.killTweensOf('.scroll-bar');

  gsap.set('#hero-text-container', { opacity: 1 });
  window.dispatchEvent(new CustomEvent('heroTextRevealed'));


      const tl = gsap.timeline({
        onComplete: () => {
          textAnimationDone = true;

          // Apagamos la scroll-zone vieja, ya cumplió su función
          if (scrollZone) {scrollZone.style.pointerEvents = 'none';}

          // Recién ahora habilitamos los clicks en tags / professions del hero
          gsap.set('#hero-text-container', { pointerEvents: 'auto' });
          heroMobileInteractive = true; // ← recién ahora se habilitan los links
          
          // Y liberamos el scroll nativo de la página
          liberarScrollNativo();
        }
      });

      tl.fromTo('#hero-eyebrow',  { opacity: 0, y: 15 }, { opacity: 1, y: 0, duration: 0.4, ease: 'power2.out' })
        .fromTo('#hero-name',     { opacity: 0, y: 25 }, { opacity: 1, y: 0, duration: 0.6, ease: 'power3.out' }, '-=0.15')
        .fromTo('#hero-alias',    { opacity: 0, y: 15 }, { opacity: 1, y: 0, duration: 0.4, ease: 'power2.out' }, '-=0.25')
        .fromTo('#hero-tagline',  { opacity: 0, y: 15 }, { opacity: 1, y: 0, duration: 0.4, ease: 'power2.out' }, '-=0.2')
        .fromTo('#hero-tools',    { opacity: 0 },          { opacity: 1, duration: 0.4, ease: 'power1.out' },       '-=0.15')
        .fromTo('#hero-nav-links',{ opacity: 0 },          { opacity: 1, duration: 0.4, ease: 'power1.out' },       '-=0.1');
    }

    function liberarScrollNativo() {
      document.documentElement.classList.remove('lock-scroll');
      document.body.classList.remove('lock-scroll');
    }

    // ─── DETECCIÓN DE INTERACCIÓN EN LA SCROLL ZONE ────────────────────────
    // Tanto un toque (touchstart) como un intento de scroll (touchmove)
    // disparan la revelación. Una vez revelado, esta zona deja de hacer nada
    // (su pointer-events se apaga al terminar la animación).
    scrollZone.addEventListener('touchstart', () => {
      if (!heroRevealed) revealHeroMobile();
    }, { passive: true });

    scrollZone.addEventListener('touchmove', () => {
      if (!heroRevealed) revealHeroMobile();
    }, { passive: true });

    scrollZone.addEventListener('click', (e) => {
      e.preventDefault();
      e.stopPropagation();

      if (!heroRevealed) {
        revealHeroMobile();
      } else if (textAnimationDone) {
        document.querySelector('section:not(#hero-section)')?.scrollIntoView({ behavior: 'smooth' });
      }
    });


    // ─── HINT DE TAP INICIAL + FLECHAS CON GLOW ──────────────────────────
    const tapHint = document.getElementById('hero-tap-hint');

    // El hint aparece con un pequeño delay para no pisar el load
    gsap.to('#hero-tap-hint', { opacity: 1, duration: 0.6, delay: 0.6, ease: 'power2.out' });

    let particlesInteracted = false;
    function onFirstScreenTouch() {
     if (particlesInteracted) return;
     particlesInteracted = true;
     window.removeEventListener('touchstart', onFirstScreenTouch);

     // El dedo desaparece apenas el usuario toca la pantalla
     if (tapHint) {
     gsap.to(tapHint, {
      opacity: 0,
      scale: 0.85,
      duration: 0.35,
      ease: 'power2.out',
      onComplete: () => tapHint.remove()
    });
  }

  // 3s después, aparecen las flechas: más grandes y con brillo pulsante
    glowDelayedCall = gsap.delayedCall(2, () => {
    if (heroRevealed) return; // por si el texto ya se reveló mientras esperábamos
    document.getElementById('hero-scroll-zone')?.classList.add('scroll-zone-glow');
    gsap.fromTo('.scroll-bar',
      { opacity: 0, scaleY: 0.85 },
      { opacity: 1, scaleY: 1.3, duration: 0.6, ease: 'back.out(1.7)', stagger: 0.08, transformOrigin: 'bottom' }
    );
  });
}
window.addEventListener('touchstart', onFirstScreenTouch, { passive: true });
} // <--- cierra el else de mobile

  // ─── SCROLLTRIGGER MATCHMEDIA (solo para el fade de salida del TEXTO en mobile) ────
  ScrollTrigger.matchMedia({
    "(max-width: 767px)": function() {
      ScrollTrigger.create({
        trigger: '#hero-section',
        start: 'top top',
        end: '+=80%',
        scrub: true,
        onUpdate(self) {
          const clampedProgress = Math.min(Math.max(self.progress, 0), 1);

          // El texto se va desvaneciendo a medida que el usuario sigue scrolleando
          gsap.set('#hero-text-container', { opacity: 1 - clampedProgress });

          // Nota: el fade de las partículas ahora lo maneja el canvas global
          // en Base.astro, no este componente. Una vez reveladas en mobile,
          // las partículas quedan atenuadas y sin interactividad para el
          // resto de la sesión (no se reactivan al volver a subir al hero).
        }
      });
    }
  });

  // ─── FILTROS DE TRABAJOS ──────────────────────────────────────────────

  // ─── TAGLINE: siempre lleva a works SIN filtro ─────────────────
document.getElementById('hero-tagline')?.addEventListener('click', () => {
  if (!heroMobileInteractive) return;
  window.dispatchEvent(new CustomEvent('worksGoToAll'));
});


  document.querySelectorAll('[data-link-category]').forEach(el => {
    el.addEventListener('click', () => {
      if (!heroMobileInteractive) return;
      window.dispatchEvent(new CustomEvent('worksFilter', {
        detail: { tool: el.getAttribute('data-link-category') }
      }));
    });
  });

  document.querySelectorAll('[data-tag-tool]').forEach(el => {
    el.addEventListener('click', () => {
      if (!heroMobileInteractive) return;
      window.dispatchEvent(new CustomEvent('worksFilter', {
        detail: { tool: el.getAttribute('data-tag-tool') }
      }));
    });
  });

  // ─── LINKS DE NAVEGACIÓN: SOBRE MÍ (scroll suave) ─────────────
  document.querySelectorAll('[data-scroll-target]').forEach(el => {
    el.addEventListener('click', () => {
      if (!heroMobileInteractive) return;
      const targetId = el.getAttribute('data-scroll-target');
      const target = document.getElementById(targetId!);
      target?.scrollIntoView({ behavior: 'smooth' });
    });
  });

  // ─── LINK DE NAVEGACIÓN: CONTACTO (Antes abría el modal, AHORA hace scroll suave) ───
  document.querySelectorAll('[data-open-contact-modal]').forEach(el => {
    el.addEventListener('click', () => {
      if (!heroMobileInteractive) return;
      const target = document.getElementById('contacto-section');
      target?.scrollIntoView({ behavior: 'smooth' });
    });
  });

</script>